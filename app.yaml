
runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app
runtime_config:
  python_version: 3.7
env_variables:
  GOOGLE_APPLICATION_CREDENTIALS: /creds/gradish-service-account.json
manual_scaling:
  instances: 1
